<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Definir categoría del módulo -->
        <record id="module_category_documents" model="ir.module.category">
            <field name="name">Documentos</field>
            <field name="sequence">10</field>
        </record>
        
        <!-- Grupo de Usuario -->
        <record id="group_documentos_user" model="res.groups">
            <field name="name">Usuario</field>
            <field name="category_id" ref="module_category_documents"/>
        </record>

        <!-- Grupo de Administrador -->
        <record id="group_documentos_admin" model="res.groups">
            <field name="name">Administrador</field>
            <field name="category_id" ref="module_category_documents"/>
            <field name="implied_ids" eval="[(4, ref('group_documentos_user'))]"/>
        </record>
    </data>
</odoo>